<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>online.pavelusanli</groupId>
    <artifactId>cv-pdf-generator</artifactId>
    <version>1.0.0</version>
    <name>cv-pdf-generator</name>
    <description>Spark Java resume preview and PDF generator</description>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- Fixes the build's own timestamps so repeated builds of one commit are
             byte-identical. The value only has to be stable, not current. -->
        <project.build.outputTimestamp>2026-09-08T00:00:00Z</project.build.outputTimestamp>

        <compiler.plugin.version>3.16.0</compiler.plugin.version>
        <surefire.plugin.version>3.6.0</surefire.plugin.version>
        <enforcer.plugin.version>3.6.3</enforcer.plugin.version>
        <exec.plugin.version>3.6.3</exec.plugin.version>
        <freemarker.version>2.3.35</freemarker.version>
        <spark.version>2.9.4</spark.version>
        <slf4j.version>2.0.19</slf4j.version>
        <openhtmltopdf.version>1.0.10</openhtmltopdf.version>
        <junit.version>6.1.3</junit.version>
    </properties>

    <!-- spark-core asks for slf4j-api 1.7.25 and slf4j-simple for 1.7.36. Both sit at
         depth 2, so Maven's nearest-wins tie-break went to whichever was declared first
         — the binding ran against the older API by accident of ordering. Pinning it to
         the same property as the binding makes the pair move together, including when
         Dependabot bumps that property. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.sparkjava</groupId>
            <artifactId>spark-core</artifactId>
            <version>${spark.version}</version>
        </dependency>
        <dependency>
            <groupId>org.freemarker</groupId>
            <artifactId>freemarker</artifactId>
            <version>${freemarker.version}</version>
        </dependency>
        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-pdfbox</artifactId>
            <version>${openhtmltopdf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Fails the build immediately, with a readable message, when it is run on
                 the wrong JDK. Without it a JDK below 25 fails later inside the compiler
                 with "invalid target release", which reads like a project misconfiguration
                 rather than the wrong java on PATH. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${enforcer.plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-build-environment</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[25,)</version>
                                    <message>This project targets Java 25. Set JAVA_HOME to a JDK 25 or newer.</message>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9,)</version>
                                </requireMavenVersion>
                                <!-- A dependency present in two versions resolves to
                                     whichever Maven picks, which is not necessarily the
                                     one that was tested. -->
                                <dependencyConvergence/>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler.plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <compilerArgs>
                        <!-- Surface deprecation and unchecked warnings rather than the
                             one-line "recompile with -Xlint" summary. -->
                        <arg>-Xlint:all,-processing</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${exec.plugin.version}</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.plugin.version}</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <!-- The integration test binds a real port and renders a PDF. A build
                         that silently runs zero tests is the failure mode to guard here:
                         it looks identical to a passing build. -->
                    <failIfNoSpecifiedTests>true</failIfNoSpecifiedTests>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
