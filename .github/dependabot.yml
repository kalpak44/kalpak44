# Managed by homelab-infra — terraform/github/kalpak44/dependabot.yml
# Edits made here in the target repo are overwritten by `just deploy github kalpak44`.
#
# Opens the dependency PRs that ai-pr-agent.yml then reviews, repairs and merges. The
# agent sweeps daily at 06:00 UTC, so these run at 05:00 to give it something to find on
# the same morning.
#
# One entry per manifest actually in the tree — a `directory` that holds no manifest is
# not an error Dependabot reports anywhere; it just silently never opens a PR:
#
#   /web-page-app package.json + package-lock.json
#   /cv-pdf-generator pom.xml
#   /web-page-app/docker Dockerfile
#
# `github-actions` is deliberately absent. This repo's workflows are generated by
# homelab-infra (terraform/github/kalpak44/workflows/), so an action bump merged here is
# reverted by the next `just deploy github kalpak44` and then re-opened by Dependabot on
# the following run. Action versions in those files are bumped in homelab-infra instead.
# Dependabot cannot watch them there either: it only scans `.github/workflows/`, and in
# homelab-infra these are ordinary files under `terraform/`.
version: 2

updates:
  - package-ecosystem: npm
    directory: /web-page-app
    schedule:
      interval: weekly
      day: monday
      time: "05:00"
      timezone: Etc/UTC
    # The agent's goal is an empty queue, and it has no per-run limit, so this is
    # sized to not withhold proposals. It throttles how many sit open at once, not how
    # many exist.
    open-pull-requests-limit: 10
    # Dependabot proposes only DIRECT dependencies unless told otherwise; a transitive
    # package would otherwise move only when an advisory forced it. `all` includes
    # indirect ones, which for npm are lockfile-only changes and fold into the group
    # below rather than arriving as separate PRs.
    allow:
      - dependency-type: all
    # Minor and patch bumps arrive as one PR, so the lockfile is resolved at once. Majors
    # are deliberately NOT in this group: they arrive as one PR each, which is what makes
    # a break attributable to a single package. They are still proposed — slf4j 1.7 to
    # 2.0 and junit 5 to 6 both came through this way. Do not add `major` here to "enable
    # majors"; that only batches them.
    groups:
      npm-minor-and-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch

  - package-ecosystem: maven
    directory: /cv-pdf-generator
    schedule:
      interval: weekly
      day: monday
      time: "05:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 10
    # Maven's transitive tree is where the convergence problems live — spark-core's
    # slf4j-api is exactly such a case — so proposing indirect updates is worth the noise.
    allow:
      - dependency-type: all
    groups:
      maven-minor-and-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch

  - package-ecosystem: docker
    directory: /web-page-app/docker
    schedule:
      interval: weekly
      day: monday
      time: "05:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 10
    ignore:
      # The maven tag encodes the JDK (`3.9.16-eclipse-temurin-25`), and Java 25 is an
      # LTS chosen deliberately. Dependabot treats the whole tag as the version and will
      # happily propose `-temurin-26`; it did, within a minute of this file landing. That
      # is not expressible as a semver-major ignored, because maven's own major is still
      # 3 — so the whole image is held here, and the pin moves by hand when we choose the
      # next LTS. It is a build-stage tool that never ships, so a stale patch is inexpensive.
      - dependency-name: maven
      # Node 24 is the LTS and is what verify-web runs, so the shipped bundle must be
      # built on it too. Dependabot proposed 26, and the agent merged it, which left the
      # bundle built on a runtime CI never exercised — held here for the same reason.
      - dependency-name: node
